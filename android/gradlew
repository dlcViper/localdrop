#!/bin/sh
# Minimal gradle wrapper shim - relies on system gradle or lets flutter handle it
exec gradle "$@" 2>/dev/null || exec ${ANDROID_HOME:-/usr/lib/android-sdk}/tools/bin/gradle "$@" 2>/dev/null || echo "Gradle wrapper cannot find gradle. Flutter Action will install it."
